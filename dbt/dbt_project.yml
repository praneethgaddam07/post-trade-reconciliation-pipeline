name: posttrade_gold
version: "1.0.0"
config-version: 2

profile: posttrade_gold

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  drift_tolerance: 0.000001

models:
  posttrade_gold:
    staging:
      +materialized: view
    marts:
      +materialized: table
